[project]
name = "hades"
revision = "1.0"

[profiles.default]
# Declare per-service secrets here as services are migrated onto hades, e.g.:
# ADGUARD_ADMIN_PASSWORD = { description = "AdGuard Home admin password", required = true }
